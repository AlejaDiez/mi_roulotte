---
import type { BaseProps } from "@utils/props";

export type Props = BaseProps;

const { id, class: cls } = Astro.props;
---

<main
    {id}
    class:list={cls}
    class="main px-space gap-space flex h-auto w-full flex-col items-start justify-start">
    <slot />
</main>
