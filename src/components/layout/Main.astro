---
import type { BaseProps } from "@utils/props";

export type Props = BaseProps;

const { id, class: cls } = Astro.props;
---

<main
    {id}
    class:list={cls}
    class="main flex h-auto w-full flex-col items-start justify-start gap-(--space-between) px-(--space-x)">
    <slot />
</main>
